Please act as a Digital Design Engineer.

**Files to Reference (Copy content of these into the model context):**
*   `source/TempSense_Control.v`
*   `source/Temperature_Buffer.v`

**Goal:**
Document the Temperature Sensor Control peripheral.

**Input Files:**
*   `source/TempSense_Control.v`
*   `source/Temperature_Buffer.v`

**Task:**
Create a file `docs/peripherals/temp_sense.md`.

**Requirements:**
1.  **Functional Description**:
    *   Explain how the `TempSense` block works (One-shot run mode).
    *   Describe the interaction with `ENMONTSENSE`.
2.  **Buffer Logic**:
    *   Explain how `Temperature_Buffer` stores the result.
3.  **IO Listing**:
    *   Create a table of inputs and outputs for both modules.

**Output:**
A complete Markdown file `docs/peripherals/temp_sense.md`.
