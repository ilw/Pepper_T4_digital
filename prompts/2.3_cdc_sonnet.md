
**Model:** Claude 3.5 Sonnet / Claude 3 Opus
**Task:** 2.3 CDC & Synchronization

**Context:**
Clock Domain Crossing is critical to getting right. We need to document the synchronizers used.

**Files to Reference (Copy content of these into the model context):**
*   `source/CDC_sync.v`

**Prompt to copy-paste:**
```markdown
Please act as a Digital Design Expert.

**Goal:**
Document the `CDC_sync` module.

**Input File:**
`source/CDC_sync.v`

**Task:**
Create a file `docs/core_blocks/cdc_sync.md`.

**Requirements:**
1.  **Mechanism**:
    *   Explain what type of synchronizer this is (likely 2-flip-flop).
    *   Does it handle level synchronization or pulse toggling? Analyze the code to be sure.
2.  **Constraints (CRITICAL)**:
    *   Calculate or state the rule for input pulse width relative to the destination clock period to guarantee capture.
    *   Add a **Warning Box** (using proper Markdown/Admonition syntax) about this constraint.
3.  **Diagram**:
    *   **Mermaid Graph**: Show the flip-flop chain `[DFF] -> [DFF]`.

**Output:**
A complete Markdown file.
```
