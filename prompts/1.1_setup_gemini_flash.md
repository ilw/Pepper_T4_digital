
**Model:** Gemini 3 Flash / Pro
**Task:** 1.1 Setup Documentation Project

**Context:**
You are setting up the documentation for the Pepper_T4 Verilog project. We are using `mkdocs` with the `material` theme.

**Goal:**
Initialize the key documentation files.

**Prompt to copy-paste:**
```markdown
Please act as an expert Technical Writer.

We need to initialize the documentation structure for a Verilog project named "Pepper_T4".
We will use MkDocs with the Material theme.

**Task:**
1.  Create a `mkdocs.yml` file in the root directory `c:\Users\ian_l\Dropbox (UK Dementia Research Institute)\Box Sync\MINT\Design\Verilog\Pepper_T4\code`.
    *   Site Name: "Pepper_T4 Digital"
    *   Theme: "material"
    *   Features:
        *   Navigation (tabs)
        *   Search
    *   Plugins:
        *   `search`
    *   Markdown Extensions:
        *   `pymdownx.highlight` (for code blocks)
        *   `pymdownx.superfences` (for diagrams)
        *   `pymdownx.arithmatex` (math)

2.  Create a `docs/` directory.
3.  Create a `docs/index.md` with a placeholder title "Pepper_T4 Digital Documentation" and a brief "Under Construction" note.
4.  Create a `docs/images/` directory.

Output the content for `mkdocs.yml` and `docs/index.md` in code blocks.
```
