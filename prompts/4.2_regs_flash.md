
**Model:** Gemini 3 Flash
**Task:** 4.2 Configuration Registers Documentation

**Context:**
We need a polished register map.

**Files to Reference (Copy content of these into the model context):**
*   `source/Configuration_Registers.v`
*   (Optional) `pepper_t4_digital_requirements(Register map).csv` if available for descriptions.

**Prompt to copy-paste:**
```markdown
Please act as a Technical Writer.

**Files to Reference (Copy content of these into the model context):**
*   `source/Configuration_Registers.v`
*   (Optional) `pepper_t4_digital_requirements(Register map).csv` if available for descriptions.

**Goal:**
Document the `Configuration_Registers` module.

**Input File:**
`source/Configuration_Registers.v`

**Task:**
Create a file `docs/peripherals/configuration_registers.md`.

**Requirements:**
1.  **Register Map Table**:
    *   Create a clean Markdown table.
    *   Columns: **Address, Register Name, Type (RW/RO), Default Value, Description**.
    *   Extract the address limitations and default values directly from the Verilog code.
2.  **Bit Fields**:
    *   For registers with specific named bits (like `GLOBAL_ENABLE`), detail what each bit does.

**Output:**
A complete Markdown file.
```
